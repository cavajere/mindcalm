# Database
DATABASE_URL=postgresql://mindcalm:mindcalm@localhost:5435/mindcalm

# Server
PORT=3300
NODE_ENV=development
TRUST_PROXY=false

# Auth
JWT_SECRET=cambia-con-stringa-casuale-di-almeno-32-caratteri
JWT_EXPIRES_IN=1h

# Storage
AUDIO_STORAGE_PATH=./storage/audio
HLS_STORAGE_PATH=./storage/hls
IMAGES_STORAGE_PATH=./storage/images
MAX_AUDIO_SIZE_MB=100
MAX_IMAGE_SIZE_MB=5
FFMPEG_PATH=

# CORS
CORS_ORIGIN=http://localhost:5473,http://localhost:5474

# Rate limiting
RATE_LIMIT_PUBLIC=100
RATE_LIMIT_LOGIN=30

# Bootstrap admin
# Valido solo finché non esiste almeno un admin attivo nel DB.
ADMIN_NAME=MindCalm Bootstrap
ADMIN_EMAIL=admin@admin.com
ADMIN_PASSWORD=admin

# SMTP (MailHog in dev — UI su http://localhost:3326)
SMTP_HOST=localhost
SMTP_PORT=3325
SMTP_FROM_EMAIL=noreply@mindcalm.local

# Frontend rendering mode
# spa = backend serve static frontend bundle
# ssr = backend proxy public routes verso Nuxt SSR
FRONTEND_RENDER_MODE=spa
FRONTEND_SSR_ORIGIN=http://localhost:5573

# Nuxt SSR: abilita indicizzazione (false per staging)
NUXT_PUBLIC_ALLOW_INDEXING=true
# Token condiviso tra API e frontend-ssr per bypass rate limit interno
SSR_INTERNAL_TOKEN=
